/// <reference types="node" />
import { EventEmitter } from "events";
import { BaseMachine, MachineCreator } from "./base";
import { EngineCreator } from "./engine";
import { BaseEvent } from "./events";
import { Reducer } from "./store";
/**
 * Combines state + side-effect handling.
 */
export declare class Machine<TState extends any, TEvent extends BaseEvent<any, any>> implements BaseMachine<TState, TEvent> {
    private _engine;
    private _store;
    constructor(reduceState: Reducer<TState, TEvent>, createEngine: EngineCreator<TState, TEvent>, initialState: TState);
    getState(): TState;
    dispatch: (event: TEvent) => void;
    onStateChange(listener: (TState: any) => void): () => EventEmitter;
    dispose(): void;
}
export declare const machineCreator: <TState extends unknown, TEvent extends BaseEvent<any, any>>(reduce: Reducer<TState, TEvent>, createEngine: EngineCreator<TState, TEvent>) => MachineCreator<TState, TEvent>;
