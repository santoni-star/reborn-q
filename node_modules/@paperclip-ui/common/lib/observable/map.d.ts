/// <reference types="node" />
import { EventEmitter } from "events";
export declare class ObservableMap<TState> {
    private _state;
    private _em;
    constructor(initialState: TState, bind?: (state: TState) => void);
    getState(): TState;
    dispose(): void;
    bind<TSelectedValue extends any = TState>(listener: (state: TSelectedValue) => void, select?: (state: TState) => TSelectedValue): () => EventEmitter;
    onChange<TSelectedValue extends any = TState>(listener: (newState: TSelectedValue, oldState: TSelectedValue) => void, select?: (state: TState) => TSelectedValue): () => EventEmitter;
    update(updater: (state: TState) => TState): void;
}
