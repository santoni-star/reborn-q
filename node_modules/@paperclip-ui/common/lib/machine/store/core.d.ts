/// <reference types="node" />
import { BaseEvent } from "../events";
import { Reducer } from "./base";
declare type ChangeListener<TState> = (state: TState) => void;
export declare class Store<TState extends Object, TEvent extends BaseEvent<any, any>> {
    private _reduceState;
    private _initialState;
    private _inner;
    constructor(_reduceState: Reducer<TState, TEvent>, _initialState: TState);
    getState(): TState;
    dispose(): void;
    dispatch(event: TEvent): void;
    onStateChange(listener: ChangeListener<TState>): () => import("events");
}
export {};
