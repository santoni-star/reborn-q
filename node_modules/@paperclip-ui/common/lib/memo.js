"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.underchange = exports.reuser = exports.shallowEquals = exports.memoize = void 0;
const LRU = __importStar(require("lru-cache"));
const DEFAULT_LRU_MAX = 10000;
// need this for default arguments
const getArgumentCount = (fn) => {
    const str = fn.toString();
    const params = str.match(/\(.*?\)|\w+\s*=>/)[0];
    const args = params
        .replace(/[=>()]/g, "")
        .split(/\s*,\s*/)
        .filter((arg) => arg.substr(0, 3) !== "...");
    return args.length;
};
const memoize = (fn, lruMax = DEFAULT_LRU_MAX, argumentCount = getArgumentCount(fn)) => {
    if (argumentCount == Infinity || isNaN(argumentCount)) {
        throw new Error(`Argument count cannot be Infinity, 0, or NaN.`);
    }
    if (!argumentCount) {
        console.error(`Argument count should not be 0. Defaulting to 1.`);
        argumentCount = 1;
    }
    return compilFastMemoFn(argumentCount, lruMax > 0)(fn, new LRU({ max: lruMax }));
};
exports.memoize = memoize;
const shallowEquals = (a, b) => {
    const toa = typeof a;
    const tob = typeof b;
    if (toa !== tob) {
        return false;
    }
    if (toa !== "object" || !a || !b) {
        return a === b;
    }
    if (Object.keys(a).length !== Object.keys(b).length) {
        return false;
    }
    for (const key in a) {
        if (a[key] !== b[key]) {
            return false;
        }
    }
    return true;
};
exports.shallowEquals = shallowEquals;
const reuser = (lruMax = DEFAULT_LRU_MAX, getKey, equals = exports.shallowEquals) => {
    const cache = new LRU({ max: lruMax });
    return (value) => {
        const key = getKey(value);
        if (!cache.has(key) || !equals(cache.get(key), value)) {
            cache.set(key, value);
        }
        return cache.get(key);
    };
};
exports.reuser = reuser;
const _memoFns = {};
const compilFastMemoFn = (argumentCount, acceptPrimitives) => {
    const hash = "" + argumentCount + acceptPrimitives;
    if (_memoFns[hash]) {
        return _memoFns[hash];
    }
    const args = Array.from({ length: argumentCount }).map((v, i) => `arg${i}`);
    let buffer = `
  return function(fn, keyMemo) {
    var memo = new WeakMap();
    return function(${args.join(", ")}) {
      var currMemo = memo, prevMemo, key;
  `;
    for (let i = 0, n = args.length - 1; i < n; i++) {
        const arg = args[i];
        buffer += `
      prevMemo = currMemo;
      key      = ${arg};
      ${acceptPrimitives
            ? `if ((typeof key !== "object" || !key) && !(key = keyMemo.get(${arg}))) {
        keyMemo.set(${arg}, key = {});
      }`
            : ""}
      if (!(currMemo = currMemo.get(key))) {
        prevMemo.set(key, currMemo = new WeakMap());
      }
    `;
    }
    const lastArg = args[args.length - 1];
    buffer += `
      key = ${lastArg};
      ${acceptPrimitives
        ? `
      if ((typeof key !== "object" || !key) && !(key = keyMemo.get(${lastArg}))) {
        keyMemo.set(${lastArg}, key = {});
      }`
        : ""}

      if (!currMemo.has(key)) {
        try {
          currMemo.set(key, fn(${args.join(", ")}));
        } catch(e) {
          throw e;
        }
      }

      return currMemo.get(key);
    };
  };
  `;
    return (_memoFns[hash] = new Function(buffer)());
};
/**
 * Calls target function once & proxies passed functions
 * @param fn
 */
const underchange = (fn) => {
    let currentArgs = [];
    let ret;
    let started;
    const start = () => {
        if (started) {
            return ret;
        }
        started = true;
        return (ret = fn(...currentArgs.map((a, i) => (...args) => currentArgs[i](...args))));
    };
    return ((...args) => {
        currentArgs = args;
        return start();
    });
};
exports.underchange = underchange;
//# sourceMappingURL=memo.js.map