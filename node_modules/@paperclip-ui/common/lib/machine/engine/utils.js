"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.combineEngineCreators = void 0;
const combineEngineCreators = (...engineCreators) => (dispatch) => {
    const engines = engineCreators.map((createEngine) => createEngine(dispatch));
    const handleEvent = (event, currState, prevState) => engines.forEach((engine) => {
        engine.handleEvent(event, currState, prevState);
    });
    const dispose = () => engines.forEach((engine) => {
        engine.dispose();
    });
    return {
        handleEvent,
        dispose,
    };
};
exports.combineEngineCreators = combineEngineCreators;
//# sourceMappingURL=utils.js.map