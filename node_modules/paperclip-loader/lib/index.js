var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var _a = require("./main.node"), startLoader = _a.startLoader, compileFile = _a.compileFile;
var DEFAULT_CONFIG_FILE_NAME = "paperclip.config.json";
var _loader;
var getLoader = function (directory, configFileName) {
    if (!_loader) {
        _loader = startLoader(directory, configFileName);
    }
    return _loader;
};
module.exports = function (source) {
    this.cacheable();
    var callback = this.async();
    var options = __assign({}, this.getOptions());
    var _a = options.configFile, configFile = _a === void 0 ? DEFAULT_CONFIG_FILE_NAME : _a;
    var loader = getLoader(process.cwd(), configFile);
    var compiledFiles = compileFile(loader, this.resourcePath);
    var mainContent;
    for (var compiledFilePath in compiledFiles) {
        if (/\.js$/.test(compiledFilePath)) {
            mainContent = compiledFiles[compiledFilePath];
        }
    }
    callback(null, mainContent);
};
