export const preventDefault = (event) => {
    event.stopPropagation();
    event.preventDefault();
    return false;
};
export const ATTR_ALIASES = {
    className: "class",
};
export const traverseNativeNode = (element, each, cpath = []) => {
    each(element, cpath);
    for (let i = 0, { length } = element.childNodes; i < length; i++) {
        const child = element.childNodes[i];
        traverseNativeNode(child, each, [...cpath, i]);
    }
};
export const arraySplice = (array, index, deleteCount, ...newValues) => {
    return [
        ...array.slice(0, index),
        ...newValues,
        ...array.slice(index + deleteCount),
    ];
};
//# sourceMappingURL=misc.js.map