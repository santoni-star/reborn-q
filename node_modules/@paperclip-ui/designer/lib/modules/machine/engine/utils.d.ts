import { BaseEvent, Dispatch } from "../events";
import { Engine, EngineCreator } from "./base";
export declare const combineEngineCreators: <TState extends unknown, TEvent extends BaseEvent<any, any>>(...engineCreators: EngineCreator<TState, TEvent>[]) => (dispatch: Dispatch<TEvent>) => Engine<TState, TEvent>;
