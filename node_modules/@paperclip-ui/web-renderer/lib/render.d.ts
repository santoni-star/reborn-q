import * as html from "@paperclip-ui/proto/lib/virt/html_pb";
import * as css from "@paperclip-ui/proto/lib/virt/css_pb";
import { NodeFactory } from "./node-factory";
import { UrlResolver } from "./native";
export declare type FrameInfo = {
    container: HTMLElement;
};
declare type RenderImport = {
    path: string;
    index: number;
    css: css.Document.AsObject;
};
declare type RenderInfo = {
    html: html.Document.AsObject;
    css: css.Document.AsObject;
    imports: RenderImport[];
};
export declare type RenderFrameOptions = {
    showSlotPlaceholders?: boolean;
    domFactory: NodeFactory;
    resolveUrl?: UrlResolver;
};
export declare const renderFrames: (info: RenderInfo, options: RenderFrameOptions) => HTMLElement[];
export declare const renderFrame: (info: RenderInfo, index: number, options: RenderFrameOptions) => HTMLElement;
export declare const patchFrames: (frames: HTMLElement[], prev: RenderInfo, curr: RenderInfo, options: RenderFrameOptions) => HTMLElement[];
export declare const patchFrame: (frame: HTMLElement, index: number, prev: RenderInfo, curr: RenderInfo, options: RenderFrameOptions) => void;
export declare const getFrameRects: (mount: HTMLElement, info: RenderInfo, index: number) => Record<string, any>;
export {};
