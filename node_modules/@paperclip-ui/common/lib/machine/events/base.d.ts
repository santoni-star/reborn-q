/**
 * Base event for event sourcing (https://martinfowler.com/eaaDev/EventSourcing.html)
 */
export declare type BaseEvent<TType extends string, TPayload = unknown> = {
    type: TType;
    payload?: TPayload;
};
export declare type Dispatch<TEvent extends BaseEvent<any, any>> = (event: TEvent) => void;
